# ---- MongoDB (optional — can also be set via the frontend) ----
# MONGO_URI=mongodb://localhost:27017
# DATABASE_NAME=mydb
# COLLECTION_NAME=mycollection

# ---- LLM / AI Integration (Google Gemini) ----
# Get your API key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_api_key_here
GEMINI_MODEL=gemini-2.0-flash

# Parser mode: "auto" (LLM first, rule-based fallback),
#              "llm"  (LLM only),
#              "rule"  (rule-based only — ignores API key)
PARSER_MODE=auto
